'use client'
import React from 'react'

const PageClient: React.FC = () => {
  /* Example to force header theme, if needed:
   * useHeaderThemeOverride('light')
   */
  return <React.Fragment />
}

export default PageClient
