const config = {
  plugins: {
    /* if you still need @import() support: */

    /* the NEW Tailwind v4 PostCSS plugin: */
    '@tailwindcss/postcss': {},

    /* you can remove autoprefixer now – Tailwind v4 handles vendor-prefixing */
    // autoprefixer: {},
  },
}

export default config
